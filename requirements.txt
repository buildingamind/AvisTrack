# Core
numpy
opencv-python
scipy
pyyaml
pydantic>=2.0
Pillow              # PIL — used by ROI picker for panel rendering
google-cloud-vision # Google Cloud Vision OCR (time calibration)

# Tracking / ML
ultralytics         # YOLO
filterpy            # Kalman filter (used by yolo realtime backend)
torch
torchvision

# Offline batch processing
pandas
polars

# Optional: DLC backend
# deeplabcut

# Dev / testing
pytest
